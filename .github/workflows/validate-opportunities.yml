name: Validate opportunities and portfolio

on:
  pull_request:
    paths:
      - "opportunities/**"
      - "portfolio/**"
      - "scripts/validate_opportunities.py"
      - "scripts/msfi_calculator.py"
      - "scripts/test_msfi_audit.py"
      - "metrics/**"
      - ".github/workflows/validate-opportunities.yml"
  push:
    branches:
      - master
    paths:
      - "opportunities/**"
      - "portfolio/**"
      - "scripts/validate_opportunities.py"
      - "scripts/msfi_calculator.py"
      - "scripts/test_msfi_audit.py"
      - "metrics/**"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run opportunity and portfolio validation
        run: python scripts/validate_opportunities.py

      - name: Run MSFI audit smoke test
        run: python scripts/test_msfi_audit.py
